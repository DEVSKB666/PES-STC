# PES Plus - Workflow คู่มือการใช้งานระบบ (ตัวอย่างกรอกจริงทีละหน้า)

เอกสารนี้แสดงขั้นตอนการทำงานของระบบ **ประเมินบุคลากร (PES Plus)** แบบเต็มรูป ตั้งแต่เริ่มต้นจนจบ พร้อมคำอธิบายว่าแต่ละหน้าในระบบต้องกรอกข้อมูลอะไร และผลลัพธ์ที่ควรเกิดขึ้น

---

## 🧩 หน้า 1: เข้าสู่ระบบ (Login)
**วัตถุประสงค์:** เพื่อเข้าสู่ระบบตามบทบาท

**ช่องกรอกข้อมูล:**
- Email: กรอกอีเมล เช่น `admin@demo.local`
- Password: กรอกรหัสผ่าน เช่น `password123`

**ปุ่ม:**
- เข้าสู่ระบบ → เรียก API `/api/auth/login`

**ผลลัพธ์:**
- เข้าหน้า Dashboard ของบทบาทนั้น ๆ
- เก็บ JWT Token เพื่อใช้เข้าถึง API ถัดไป

---

## 🧭 หน้า 2: แดชบอร์ด (Dashboard / Overview)
**วัตถุประสงค์:** แสดงข้อมูลสรุปของระบบตามบทบาท

| บทบาท | ข้อมูลที่แสดง |
|--------|----------------|
| Admin | จำนวนรอบประเมิน, ผู้รับการประเมิน, กรรมการ, ปุ่มสร้างรอบใหม่ |
| ผู้รับการประเมิน | รายการรอบเปิดอยู่, ความคืบหน้า %, ปุ่มเริ่มประเมิน |
| กรรมการ | รายการผู้รับการประเมินที่ได้รับมอบหมาย, สถานะงาน |

**API ที่เกี่ยวข้อง:**
- `/api/hr/statistics`
- `/api/evaluated/progress`
- `/api/committee/assignments`

---

## 🗓️ หน้า 3: จัดการรอบประเมิน (Admin)
**ช่องกรอกข้อมูล:**
- ชื่อรอบ: เช่น “รอบประเมินภาคเรียนที่ 1/2568”
- วันที่เริ่ม / วันที่สิ้นสุด
- สถานะเปิด/ปิด (toggle switch)

**ปุ่ม:**
- บันทึก → `POST /api/hr/cycles`
- เปิด/ปิดรอบ → `PATCH /api/hr/cycles/:id/status`

**ผลลัพธ์:**
- รายการรอบใหม่ถูกเพิ่มในตาราง

---

## 📋 หน้า 4: หัวข้อการประเมิน (Admin)
**ช่องกรอกข้อมูล:**
- ชื่อหัวข้อ: เช่น “ผลการปฏิบัติงาน”, “คุณธรรมจริยธรรม”
- น้ำหนัก: เช่น 30%

**ปุ่ม:**
- เพิ่มหัวข้อ → `POST /api/hr/topics`
- แก้ไข / ลบ → `PUT` หรือ `DELETE`

---

## 📊 หน้า 5: ตัวชี้วัด (Admin)
**ช่องกรอกข้อมูล:**
- ชื่อตัวชี้วัด: เช่น “ส่งงานตรงเวลา”
- ประเภท: (เลือก) score / file / url / text
- น้ำหนักในหัวข้อ
- คำอธิบายเพิ่มเติม

**ปุ่ม:**
- บันทึก → `POST /api/hr/indicators`

**ผลลัพธ์:**
- ตัวชี้วัดปรากฏใต้หัวข้อที่เลือก

---

## 🧑‍⚖️ หน้า 6: มอบหมายกรรมการ (Admin)
**ช่องกรอกข้อมูล:**
- เลือกรอบประเมิน
- เลือกผู้รับการประเมิน
- เลือกกรรมการ (multi-select)

**ปุ่ม:**
- บันทึก → `POST /api/hr/assignments`

**ผลลัพธ์:**
- ตารางแสดงรายการมอบหมาย (กรรมการ ↔ ผู้รับการประเมิน)

---

## 🧍‍♀️ หน้า 7: ประเมินตนเอง (Evaluatee)
**ช่องกรอกข้อมูล (ตามประเภทตัวชี้วัด):**
| ประเภท | สิ่งที่ต้องกรอก | API |
|----------|-------------------|------|
| score | เลือกคะแนน 1–4 | `/api/evaluated/self-evaluation` |
| file | อัปโหลดไฟล์ PDF, DOC, JPG | `/api/evaluated/upload-evidence` |
| url | วางลิงก์หลักฐาน | `/api/evaluated/self-evaluation` |
| text | พิมพ์ข้อความสะท้อนคิด | `/api/evaluated/self-evaluation` |

**ปุ่ม:**
- บันทึกแบบร่าง / ส่งแบบประเมิน

**ผลลัพธ์:**
- ข้อมูลถูกบันทึกในฐานข้อมูล, เปลี่ยนสถานะเป็น draft หรือ submitted

---

## 🔍 หน้า 8: ความคืบหน้า (Evaluatee)
**ข้อมูลที่แสดง:**
- ตัวชี้วัดทั้งหมด: 10 รายการ
- ทำเสร็จแล้ว: 6 รายการ (60%)

**API:** `/api/evaluated/progress/:periodId`

**ผลลัพธ์:** แสดง progress bar (%) ความคืบหน้า

---

## 💬 หน้า 9: ความเห็นกรรมการ (Evaluatee)
**ข้อมูลที่แสดง:**
- คะแนนที่กรรมการให้แต่ละข้อ
- ความเห็นรายตัวชี้วัด
- ความเห็นรวม

**API:** `/api/evaluated/comments/:periodId`

---

## 🧾 หน้า 10: Export ผลประเมิน (Evaluatee/Admin)
**ปุ่ม:** “ดาวน์โหลดผลประเมิน PDF”

**API:** `/api/reports/export/:periodId/:evaluateeId`

**ผลลัพธ์:** ได้ไฟล์ PDF รวมคะแนนตนเอง + คะแนนกรรมการ + ลายเซ็น

---

## 🧑‍⚖️ หน้า 11: คิวงานกรรมการ (Committee)
**ข้อมูลที่แสดง:**
- ผู้รับการประเมินทั้งหมดที่ได้รับมอบหมาย
- สถานะ (ยังไม่ประเมิน / ประเมินแล้ว / ลงนามแล้ว)

**คลิกเพื่อเข้าไปให้คะแนน:**
- ดู self-score, หลักฐาน, ช่องให้ความเห็น, ช่องใส่คะแนนกรรมการ

**API:** `/api/evaluator/assignments`, `/api/evaluator/evaluation-details/:assignmentId`

---

## ✍️ หน้า 12: ลงนามอิเล็กทรอนิกส์ (Committee)
**การทำงาน:**
- วาดลายเซ็นใน canvas (base64)
- ส่งข้อมูลไปยัง API `/api/evaluator/sign-evaluation`
- ปุ่มยกเลิกลงนาม → `/api/evaluator/unsign-evaluation`

**ผลลัพธ์:**
- ระบบบันทึกลายเซ็นไว้ในตาราง `signatures`

---

## 📈 หน้า 13: สรุปผลประเมิน (Admin)
**ข้อมูลที่แสดง:**
- ค่าเฉลี่ยคะแนนของแต่ละผู้ถูกประเมิน
- สถานะกรรมการลงนามครบหรือไม่

**API:** `/api/reports/summary/:periodId`

**ปุ่ม:** “Export สรุปผล (PDF)”

---

## 🔄 Workflow รวม
1. Admin → ตั้งรอบ + ตัวชี้วัด + มอบหมายกรรมการ
2. ผู้รับการประเมิน → ประเมินตนเอง + แนบหลักฐาน
3. กรรมการ → ตรวจ/ให้คะแนน/ลงนาม
4. Admin → ตรวจสรุปผล + Export รายงาน

---

## 🧪 ตัวอย่างการทดสอบ (Unit Test)
| รายการ | วิธีทดสอบ | คาดหวังผล |
|----------|-------------|-------------|
| Login | ใส่ username/password ถูก | กลับ token 200 OK |
| Insert ข้อมูล | POST /api/hr/topics | บันทึกลง DB สำเร็จ |
| Upload ไฟล์ | POST /api/evaluated/upload-evidence | ไฟล์อยู่ใน uploads/ |
| Export PDF | GET /api/reports/export/... | ได้ไฟล์ PDF ถูกต้อง |
| Permission | evaluator เข้าหน้า admin | Forbidden 403 |

---

## ✅ สรุป
ระบบนี้มีทุกฟังก์ชันตามโจทย์การแข่งขันครบ: การจัดการรอบ, หัวข้อ, ตัวชี้วัด, การประเมิน, การมอบหมาย, การให้คะแนน, การลงนาม และการส่งออกผล PDF.

